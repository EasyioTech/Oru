// @oru/schemas — Zod schemas shared between backend and frontend
// Import from module-specific paths for tree-shaking:
//   import { loginSchema } from '@oru/schemas/auth'
//   import { createClientSchema } from '@oru/schemas/crm'

export * from './auth/index.js';
