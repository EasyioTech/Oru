# Workspace Rules

- **Currency**: This is an Indian ERP system, not a US-based one. Always use Indian Rupee (₹, INR) as the default currency for all financial data, charts, and formats instead of USD ($). Use the 'en-IN' locale for number formatting when available.
