# Oru ERP Docker Compose

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: oru_erp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://postgres:admin@postgres:5432/oru_erp
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - PORT=3000
      - JWT_SECRET=JcMSILOrQE6jKwuNEORXAK1UFky8nWgU7EWVXCzNN0o=
      - VITE_JWT_SECRET=JcMSILOrQE6jKwuNEORXAK1UFky8nWgU7EWVXCzNN0o=
      - CORS_ORIGINS=https://oruerp.com,https://www.oruerp.com,https://oruerp.com,https://www.oruerp.com,http://localhost:5173,http://localhost:3000
      - NODE_ENV=production
    volumes:
      - backend_uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_started
      redis:
        condition: service_started
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.oruerp.rule=Host(`oruerp.com`, `www.oruerp.com`)"
      - "traefik.http.routers.oruerp.entrypoints=web,websecure"
      - "traefik.http.routers.oruerp.tls.certresolver=letsencrypt"
      - "traefik.http.services.oruerp.loadbalancer.server.port=3000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
  backend_uploads:
