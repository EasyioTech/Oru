{
    email easyiotechnologies@gmail.com
}

oruerp.com, get-oru.com {
    # All requests go through backend for meta tag injection
    # Backend serves /api, /sitemap.xml, /robots.txt, and frontend HTML with injected SEO tags
    handle /* {
        reverse_proxy backend:3000
    }

    # Optional: Log file
    log {
        output file /var/log/caddy/access.log
    }
}
